enum EyeExaminationsType { addPhoto, date, gallery }

enum AboutType { farco, app }

enum SignUpType { doctor, patient }

enum ExaminationsType { eyeSight, eyeFundus }

enum UpdateDoes { edite, delete }
